version: "3.9"

services:
  marpelle:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: marpelle
    working_dir: /app
    volumes:
      - .:/app
    command: python -m marpelle.cli --help
